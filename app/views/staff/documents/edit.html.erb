<% provide(:head_title, t('views.pages.edit',resource: t('views.names.document.singular'))) %>
<% provide(:page_title, raw(t('views.pages.edit',resource: t('views.names.document.singular')))) %>

<%= render 'staff/documents/form' %>
