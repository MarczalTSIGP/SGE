<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> alert alert-dismissible fade show" role="alert">
    <button class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true"></span>
    </button>
    <%= message %>
  </div>
<% end %>
