<p><%=t('devise.mailer.hello')%> <%= @resource.name %>!</p>

<p><%=t('devise.mailer.reset_password.header')%></p>

<p><%= link_to t('devise.mailer.reset_password.link'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%=t('devise.mailer.reset_password.ignore')%></p>
<p><%=t('devise.mailer.reset_password.info')%></p>
