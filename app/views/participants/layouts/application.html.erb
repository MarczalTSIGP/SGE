<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:head_title)) %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'participants/application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<div class="page">
  <div class="page-main">
    <%= render 'participants/layouts/navtop' %>
    <%= render 'participants/layouts/navbottom' %>

    <div class="container">

      <div class="page-header">
        <h1 class="page-title">
          <%= yield(:page_title) %>
        </h1>
      </div>

      <div class="row">
        <div class="col-12">

          <div class="card">
            <div class="card-body">
              <%= render 'shared/flash_messages' %>
              <%= yield %>
            </div>
          </div>

        </div>
      </div> <!-- .row -->
    </div> <!-- .container -->
  </div>
  <%= render 'participants/layouts/footer' %>
</div>
</body>
</html>
