<%= simple_form_for [:admin, @department] do |f| %>
  <fieldset class="form-fieldset">
    <legend class="w-auto"><%= t('views.fieldset.department') %></legend>
    <div class="row">
      <div class="col-sm-6">
        <%= f.input :name %>
      </div>
      <div class="col-sm-6">
        <%= f.input :local %>
      </div>
      <div class="col-sm-3">
        <%= f.input :initials %>
      </div>


      <div class="col-sm-3">
        <%= f.input :phone, class: 'form-control', input_html: { 'data-mask' => '(00) 0000-0000',
                                                                 'placeholder': "(00) 0000-0000",
                                                                 'autocomplete': 'off',
                                                                 'maxlength': '13' } %>
      </div>
      <div class="col-sm-6">
        <%= f.input :email do %>
          <div class='input-group'>
            <%= f.input_field :email, class: 'form-control' %>
            <span class='input-group-text'>@utfpr.edu.br</span>
          </div>
        <% end %>

      </div>
      <div class="col-sm-12">
        <%= f.input :description %>
      </div>
    </div>
  </fieldset>
  <div class='d-flex'>
    <%= link_to t('views.links.back'), admin_departments_path, class: 'btn btn-secondary' %>
    <%= f.button :submit, class: 'btn btn-primary ml-auto' %>
  </div>

<% end %>
