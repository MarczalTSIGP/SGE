<% provide(:head_title, t('helpers.submit.create',
                          model: t('activerecord.models.document.one'))) %>

<% provide(:page_title, t('helpers.submit.create',
                          model: t('activerecord.models.document.one'))) %>


<%= render 'form' %>
