<% provide(:head_title, t('views.pages.create', resource: t('views.names.server.singular'))) %>
<% provide(:page_title, t('views.pages.create', resource: t('views.names.server.singular'))) %>

<%= render 'form' %>
