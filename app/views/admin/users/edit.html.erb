<% provide(:head_title, t('helpers.submit.update',
                          model: t('activerecord.models.user.one'))) %>

<% provide(:page_title, t('helpers.submit.update',
                          model: "\##{@user.id}")) %>

<%= render 'form' %>

