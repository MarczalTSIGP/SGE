<% provide(:head_title, t('helpers.links.menage',
                          model: t('activerecord.models.clients_document.one'))) %>

<% provide(:page_title, t('helpers.links.menage',
                          model: t('activerecord.models.clients_document.one'))) %>


<%= render 'form' %>

<%= render 'index' %>